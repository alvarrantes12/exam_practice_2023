%h1= t('application.edit_model', model: t('activerecord.modules.event.one'))
= render "form", event: @event
%br/
%div

= link_to t('application.show'), @event
\|
= link_to t('application.back'), events_path
